//! GENERATED — DO NOT EDIT. Written from the project's one table of performance budgets, and
//! regenerated on every check, so the number here is the number the budget table states.
//!
//! The shell used to declare its own renderer ceiling as a literal here, and it was a fourth
//! reader of a number three other readers already disagreed about — the one a person actually
//! sees, in every `renderer_vitals` line. It is read rather than written now.

/// The product's own declared renderer ceiling, written into every renderer_vitals line.
/// It used to be a literal here, and it was a FOURTH reader of a number three other readers
/// already disagreed about - the one a person actually sees in the log. It reads the ruled
/// ceiling now, which sits BELOW what the shell used to promise itself, so the app no longer
/// declares more headroom than its gate allows. A LOG LINE, never a limit: the shell does not
/// kill the window it is reporting on.
pub const RENDERER_BUDGET_KB: u64 = 1_433_600;
